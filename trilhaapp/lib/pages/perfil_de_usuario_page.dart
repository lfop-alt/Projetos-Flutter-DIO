import 'package:flutter/material.dart';

class perfilDeUsuarioPage extends StatelessWidget {
  const perfilDeUsuarioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text("Perfil"),
      ),
    );
  }
}
